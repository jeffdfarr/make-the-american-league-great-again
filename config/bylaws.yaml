# The league rulebook Google Sheet (link-readable).
# PRIVACY: only these tabs are fetched; the Owners tab (phone numbers) is
# never listed here and the exporter refuses contact-looking content.
sheet_id: 1-CI4Coq19ezTv5t1GObPi-4k_qbJCK0fK1x-qTz5LyY
pages:
  rules:  { tab: "Rules",        mode: outline }
  trades: { tab: "Trades",       mode: trades }
  draft:  { tab: "Rookie Draft", mode: draftboard }
